<resources>

    <style name="AppTheme" parent="AppTheme.Base"/>

    <!-- Base application theme. -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- customize the color palette -->
        <item name="colorPrimary">@color/Material.Red</item>
        <item name="colorPrimaryDark">@color/Material.DarkRed</item>
        <item name="colorAccent">@color/Material.Blue</item>
        <item name="android:textColorPrimary">@color/Material.White</item>
    </style>

</resources>
